# RapidResponse Flask (fresh build)

## 1) Setup
```bash
cd rapidresponse_flask/backend
python -m venv .venv
# Windows: .venv\Scripts\activate
# macOS/Linux: source .venv/bin/activate
pip install -r requirements.txt
